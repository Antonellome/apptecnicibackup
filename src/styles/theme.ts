import { createTheme } from '@mui/material/styles';

// CIAO. Tema vuoto e neutro. Non applica nessuno stile personalizzato.
// Serve solo a risolvere il crash della build.
const theme = createTheme({});

export default theme;
